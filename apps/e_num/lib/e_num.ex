defmodule ENum do
  # all? -> checks if all elements in enum meet criteria
  # any? -> cehcks if at least one element in enum meet criteria
  # chunk_every -> split an enum at every x size
  # chunk_by -> split an enum based on a criteria
  # map_every -> generate new enum every x time depending on callback
  # each
  # map
  # min
  # max
  # filter
  # ! reduce if no acc is provided, then acc is first element
  # sort
  # uniq -> remove duplicates
  # uniq_by -> remove duplicates by providing criterion
  # & -> capture operator
end
